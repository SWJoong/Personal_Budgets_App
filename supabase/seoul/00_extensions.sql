-- =====================================================================
-- 00. 확장 (extensions)
--
-- 실행 순서: 00 → 01 → 02 → 03 → 04 → 05 → 06 → 07 → 08
-- 실행 위치: Supabase 대시보드 > SQL Editor (수동)
--
-- 이 디렉터리(supabase/seoul/)는 서울형 전용 앱의 **정본 빌드**입니다.
-- 기존 supabase/migrations/ 는 이 브랜치에서 사용하지 않습니다 —
-- 그쪽은 코어 테이블을 만들지 않아 빈 프로젝트에서 첫 파일부터 실패합니다.
-- 자세한 내용과 검증 재현 절차는 supabase/seoul/README.md 참조.
--
-- 모든 파일은 재실행 가능(idempotent)합니다. 중간에 실패하면 고친 뒤
-- 처음부터 다시 돌려도 됩니다.
-- =====================================================================

CREATE EXTENSION IF NOT EXISTS pgcrypto;   -- gen_random_uuid(), crypt()
